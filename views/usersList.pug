head
  meta(charset='UTF-8')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  title Users List - Young4Chicks
  link(href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css' rel='stylesheet')
  link(rel='stylesheet' href='https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css')
  link(rel='stylesheet' href='/css/style.css')
  style.
    table {
    font-family: arial, sans-serif;
    border-collapse: collapse;
    width: 100%;
    }
    td, th {
    border: 1px solid #dddddd;
    text-align: left;
    padding: 8px;
    }
    tr:nth-child(even) {
    background-color: #dddddd;
    }
h2 Sales Rep 
table
  thead 
    tr
        th Full Name 
        th Contact
        th Email 
        th Role
        th Update 
        th Delete
  tbody 
  tr
    if users.length 
       each user in users
        if user.role === 'salesRep'
          tr 
              td #{user.fullname}
              td #{user.email}
              td #{user.phone}
              td #{user.role}
              td 
                a.btn.btn-primary.btn-block(href='/updateuser/'+user._id) Update
              td 
                form(method='Post' action='/deleteuser')  
                  input(type="hidden" name='id' value=user._id)
                  button.btn.btn-danger.btn-block(type='submit') Delete 

br 
br
h2 Manager's List 
table
  thead 
    tr
        th Full Name 
        th Contact
        th Email 
        th Role
        th Update 
        th Delete
  tbody 
  tr
    if users.length 
       each user in users
        if user.role === 'brooderManager' 
          tr 
              td #{user.fullname}
              td #{user.email}
              td #{user.phone}
              td #{user.role}
              td 
                a.btn.btn-primary.btn-block(href='/updateuser/'+user._id) Update
              td 
                form(method='Post' action='/deleteuser')  
                  input(type="hidden" name='id' value=user._id)
                  button.btn.btn-danger.btn-block(type='submit') Delete 

br 
br
h2 Farmer's List 
table
  thead 
    tr
        th Full Name 
        th Contact
        th Email 
        th Role
        th Update 
        th Delete 
  tbody 
  tr
    if users.length 
       each user in users
        if user.role === 'farmers' 
          tr 
              td #{user.fullname}
              td #{user.email}
              td #{user.phone}
              td #{user.role}
              td
                a.btn.btn-primary.btn-block(href='/updateuser/'+user._id) Update
              td 
                form(method='Post' action='/deleteuser')  
                  input(type="hidden" name='id' value=user._id)
                  button.btn.btn-danger.btn-block(type='submit') Delete 
